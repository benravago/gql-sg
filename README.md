Some additional annotations and an annotation-processor to complement
the Spring-GraphQL annotations (@QueryMapping, @MutationMapping, @SubscriptionMapping, and @SchemaMapping).

The objective here is to make a code-first option that is compatible with the Spring-GraphQL schema-first approach.

Currently just a POC using junit tests to demonstrate various features.
